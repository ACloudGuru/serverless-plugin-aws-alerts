{
  "name": "serverless-plugin-aws-alerts-example",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "package:staging": "./node_modules/.bin/sls package --stage staging --verbose",
    "deploy:staging": "./node_modules/.bin/sls deploy --stage staging --verbose --force",
    "remove:staging": "./node_modules/.bin/sls remove --stage staging --verbose"
  },
  "devDependencies": {
    "serverless-plugin-aws-alerts": "^1.2.4"
  },
  "dependencies": {
    "serverless": "^2.0.0"
  }
}
