{
  "name": "serverless-plugin-aws-alerts-example",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
      "deploy:staging": "./node_modules/.bin/sls deploy --stage staging --verbose",
      "remove:staging": "./node_modules/.bin/sls remove --stage staging --verbose"
  },
  "devDependencies": {
    "serverless": "^1.8.0",
    "serverless-plugin-aws-alerts": "^1.0.0"
  }
}
